package com.amex;

import io.vertx.core.Promise;

public class TodoVerticle extends MicroServiceVerticle {
  @Override
  public void start(Promise<Void> startPromise) throws Exception {
    super.start(startPromise);
  }
}
